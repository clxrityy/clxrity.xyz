export default function HomePage() {
    return (
        <main>
            <h1>clxrity.xyz — HBD branch</h1>
            <p>Neon + Prisma, Edge API routes, OAuth2 Dashboard</p>
        </main>
    );
}
