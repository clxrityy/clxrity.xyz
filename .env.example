# Neon Postgres connection string (use pooled URL for Prisma and edge neon for edge routes)
# Recommended for most uses
DATABASE_URL=""

# For uses requiring a connection without pgbouncer
DATABASE_URL_UNPOOLED=""

# Parameters for constructing your own connection string
PGHOST=""
NEON_DATABASE_URL=""

# NextAuth
AUTH_SECRET="" # generate using `openssl rand -base64 32`
# IMPORTANT: Use ORIGIN ONLY (no path). Examples:
#   Local dev: http://localhost:3000
#   Tunnel:    https://random-subdomain.trycloudflare.com
AUTH_URL=""
NEXTAUTH_URL="" # optional, also ORIGIN ONLY; Auth.js will use AUTH_URL first if set
CF_TUNNEL_URL="" # optional, e.g. https://random-subdomain.trycloudflare.com
NEXT_PUBLIC_APP_URL="" # optional public base URL if needed

# Discord Interactions (signature verification)
DISCORD_PUBLIC_KEY=""

# OAuth providers (Discord)
DISCORD_CLIENT_ID=""
DISCORD_CLIENT_SECRET=""

# Bot token and application ID for bot-related features
DISCORD_BOT_TOKEN=""
DISCORD_APP_ID=""

# Public URLs
NEXT_PUBLIC_DISCORD_INVITE_URL="" # Invite link for your bot
NEXT_PUBLIC_GITHUB_URL="" # Link to your GitHub repo
NEXT_PUBLIC_DISCORD_SERVER_URL="" # Link to your Discord server

# Cron job secret (for /api/cron/* endpoints)
CRON_SECRET="" # generate using `openssl rand -base64 32`

# Internal interactions secret (for verifying requests from your bot)
INTERNAL_INTERACTIONS_SECRET="" # generate using `openssl rand -base64 32`

LOG_ENCRYPTION_KEY="" # generate using `openssl rand -base64 32`

GOOGLE_SITE_VERIFICATION="" # content value for Google site verification meta tag

AI_GATEWAY_API_KEY="" # API key for AI Gateway services (https://vercel.com/docs/ai-gateway)
OPENAI_API_KEY="" # OpenAI API key for accessing OpenAI services (https://platform.openai.com/)
