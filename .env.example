# Neon Postgres connection string (use pooled URL for Prisma and edge neon for edge routes)
DATABASE_URL="postgresql://user:password@ep-neon-host/db?sslmode=require"
NEON_DATABASE_URL="postgresql://user:password@ep-neon-host/db?sslmode=require"

# NextAuth
AUTH_SECRET="change-me"
AUTH_URL="http://localhost:3000"
CF_TUNNEL_URL="" # optional, e.g. https://random-subdomain.trycloudflare.com
NEXT_PUBLIC_APP_URL="" # optional public base URL if needed

# OAuth providers (Discord)
DISCORD_CLIENT_ID=""
DISCORD_CLIENT_SECRET=""
